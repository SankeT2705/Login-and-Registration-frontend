import React from "react";
import { useState } from "react";

const Tasklabel = (props) => {

    // statusCheck(status);
    return <a href="\" className={props.class}> {props.name}  </a>
}
export default Tasklabel;